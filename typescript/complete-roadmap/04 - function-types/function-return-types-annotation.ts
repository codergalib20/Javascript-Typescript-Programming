const add = (x: number, y: number): number => {
  return x + y;
};
const introduction = (name: string, age: number): string => {
  return `This is ${name}, I'm ${age} now`;
};
const isregular = (type: boolean) : boolean => {
    return type;
}